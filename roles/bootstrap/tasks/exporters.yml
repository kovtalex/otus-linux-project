---
- name: Copy project
  copy:
    src: exporters
    dest: /root
    mode: 0755

- name: Copy .env
  template:
    src: .env.j2
    dest: /root/exporters/.env
    mode: 0640

- name: Run containers
  command:
    cmd: docker-compose up -d --build
    chdir: /root/exporters
  changed_when: false
