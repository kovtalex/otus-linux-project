version: "3.8"

services:
  mysql:
    extra_hosts:
{% for host in groups['all'] %}
      - "{{ host }}:{{ hostvars[host]['ansible_eth1']['ipv4']['address'] }}"
{% endfor %}

  mysql-exporter:
    extra_hosts:
{% for host in groups['all'] %}
      - "{{ host }}:{{ hostvars[host]['ansible_eth1']['ipv4']['address'] }}"
{% endfor %}
